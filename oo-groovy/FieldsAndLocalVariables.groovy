class PretendFieldCounter {  public count = 0  // Overriding field access operator  Object get(String name) {    return 'pretend value'  }  void set(String name, Object value) {    count++  }}def pretender = new PretendFieldCounter()// get('isNoField') will get calledassert pretender.isNoField == 'pretend value'/*With the count field, you can see that it looks like the get/set methods are notused if the requested field is present.*/assert pretender.count == 0/*set('isNoFieldEither', 'just to increase counter') will get called,increasing counter field vlue.*/pretender.isNoFieldEither = 'just to increase counter'assert pretender.count == 1